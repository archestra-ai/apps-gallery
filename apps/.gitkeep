# Submissions land here, one folder per app: apps/<github-login>_<app-slug>/
# (recording.json + optional thumbnail.png). This placeholder just keeps the
# otherwise-empty directory in git; it is safe to leave in place.
