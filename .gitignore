node_modules/
# index.json is generated by CI and published to the gallery-index branch,
# never committed to main. Build it locally with `npm run build-index` if you like.
/index.json
*.log
.DS_Store
